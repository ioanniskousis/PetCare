<%= form_with(model: lost_photo) do |form| %>
  <% if lost_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lost_photo.errors.count, "error") %> prohibited this lost_photo from being saved:</h2>

      <ul>
        <% lost_photo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
