<h1>Editing Treatment</h1>

<%= render 'form', treatment: @treatment %>

<%= link_to 'Show', @treatment %> |
<%= link_to 'Back to ' + @treatment.pet.name + ' treatment', pet_treatments_path(@treatment.pet) %>
