<%= form_with(model: treatment_category) do |form| %>
  <% if treatment_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(treatment_category.errors.count, "error") %> prohibited this treatment_category from being saved:</h2>

      <ul>
        <% treatment_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :categoryName %>
    <%= form.text_field :categoryName %>
  </div>

  <div class="field">
    <%= form.label :parentCategory_id %>
    <%= form.text_field :parentCategory_id %>
  </div>

  <div class="field">
    <%= form.label :order %>
    <%= form.text_field :order %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
