<%= form_with(model: treatment_photo) do |form| %>
  <% if treatment_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(treatment_photo.errors.count, "error") %> prohibited this treatment_photo from being saved:</h2>

      <ul>
        <% treatment_photo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
